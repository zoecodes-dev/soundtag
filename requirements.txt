# SoundTag 의존성 (Python 3.12 기준)
#   설치:  pip install -r requirements.txt

# --- 핵심 파이프라인 (scripts/, tests/) ---
python-dotenv==1.2.2
numpy==1.26.4
requests==2.32.5
replicate==1.0.7
librosa==0.10.2.post1
soundfile==0.13.1
matplotlib==3.10.8
scikit-learn==1.8.0
pedalboard==0.9.22
msclap==1.3.4
essentia                 # 오디오 특성 분석 (플랫폼별 설치 까다로움 — 빌드 옵션 참고)

# --- AST 모델 실험 노트북 (notebooks/) 전용, 용량 큼 ---
torch==2.10.0
torchaudio==2.10.0
transformers==4.57.6
datasets                 # Kaggle 환경에서 사용 (버전 미고정)
evaluate                 # Kaggle 환경에서 사용 (버전 미고정)
